alert("There will generate a random numbers");
let randomNumber = Math.round(Math.random() * 99);
alert(`Randomly generated nuber is ${randomNumber}`);

confirm("Now we will multiply it by 2 and then divided it by 2 ");
alert(`The random number was ${randomNumber} We multiplied it by 2 and got ${randomNumber * 2} and we divided it by 2 and got ${randomNumber / 2} and the remainder will be ${randomNumber / 2 % 3}`);
